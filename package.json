{
  "name": "face-value",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "lint:fix": "eslint --fix",
    "schema:generate": "node -e \"fetch('https://raw.githubusercontent.com/gyorgygutai/flux-klein-worker/main/schema.json').then(r=>r.json()).then(async s=>{const {compile}=await import('json-schema-to-typescript');const req=await compile(s.request,'RequestPayload');const res=await compile(s.response,'ResponsePayload');const fs=await import('fs');fs.writeFileSync('types/schema.ts',req+'\\n'+res);})\""
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "next": "16.2.6",
    "react": "19.2.4",
    "react-dom": "19.2.4",
    "sharp": "^0.34.5"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.2.6",
    "json-schema-to-typescript": "^15.0.4",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
